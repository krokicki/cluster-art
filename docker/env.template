# Cluster Art Docker Configuration
# Copy this file to .env and customize as needed

# Path to cache directory on the host
# This should be the same directory your crontab writes to
CLUSTER_CACHE_FOLDER=/var/cache/cluster-art

# TLS certificate for Nginx
CERT_FILE=$PWD/certs/cert.crt
KEY_FILE=$PWD/certs/cert.key

